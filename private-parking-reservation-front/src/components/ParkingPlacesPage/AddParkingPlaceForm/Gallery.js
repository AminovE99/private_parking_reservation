import React, { useCallback } from 'react'

const Gallery = ({ history }) => {
    return (
        <div className='photos'>
            <span>Загруженные фотки</span>
        </div>
    )
}

export default Gallery